{% extends 'layout.html.twig' %}
{% block content %}
  <!-- Services -->
  {{ pimcore_areablock('feature', { allowed: ['feature-list-brick'] }) }}
  <!-- Portfolio Grid -->
  {{ pimcore_areablock('portfolio', { allowed: ['portfolio-list-brick'] }) }}
  <!-- Team -->
  {{ pimcore_areablock('team', { allowed: ['team-list-brick'] }) }}

{% endblock %}
